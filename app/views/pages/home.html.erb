<div class="jumbotron">
	<h1>Welcome!</h1>
	<% if user_signed_in? %>
		<!-- do something -->
	<% else %>
		<p>Wishing is a simple app to allow wishlist creation</p>
		<%= link_to "Sign Up", new_user_registration_path, class: "btn btn-danger" %>
	<% end %>
</div>